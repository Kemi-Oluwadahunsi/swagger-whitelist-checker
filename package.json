{
  "name": "swagger-whitelist-checker",
  "version": "1.1.0",
  "description": "GitHub Action to validate Swagger endpoints against a centralized whitelist",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "csv-parser": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2",
    "ts-node": "^10.9.1"
  }
}
